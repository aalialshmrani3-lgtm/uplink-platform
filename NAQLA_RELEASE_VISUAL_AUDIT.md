# تدقيق الواجهة — NAQLA Platform RC1

تم الفحص في 24 أغسطس 2026 عبر متصفح Chromium ولقطات العرض المحلية، دون جلسة مستخدم أو بيانات إنتاجية.

| النطاق | النتيجة المرئية | الحكم |
|---|---|---|
| الصفحة الرئيسية | توضح التأهيل ثم الاتصال ثم التسويق، وتعرض CTA للتشغيل وحدودًا صريحة للبيانات والقرارات البشرية | PASS |
| سطح المكتب `/naqla3` | ظهرت شاشة الإقلاع أولاً ثم حاجز تسجيل الدخول للمساحة التجارية، بلا أصل أو معاملة ظاهرة | PASS |
| الجوال 375×812 | ظهرت الصفحة الرئيسية ومسارات المطابقة ومساعدة المراجع والمتقدم بحاويات متجاوبة وعناصر تحكم ضمن العرض | PASS للحالات العامة المتاحة |
| الجهاز اللوحي 768×1024 | ظهرت الصفحة الرئيسية ومطابقة NAQLA2 ومساحات المساعدة وحاجز NAQLA3 بلا قص مرئي | PASS للحالات العامة المتاحة |
| اتجاه اللغة | اختبار DOM يثبت ضبط `html[dir]` و`lang` بين الإنجليزية LTR والعربية RTL وحفظ الاختيار | PASS |

> لا يدعي هذا التدقيق فحص واجهات بيانات مصادق عليها في متصفح الإنتاج؛ تغطيها اختبارات الواجهة المعزولة والـHTTP E2E ببيانات synthetic فقط.

## تحديث قبل الإصدار

كشف فحص `/` أن الصفحة الرئيسية الموروثة تعرض مؤشرات وشراكات وتكاملات وEscrow وBlockchain غير موثقة. استبدلت صفحة `Home.tsx` بمحتوى Release Candidate يصف مسارات NAQLA الفعلية وحدودها فقط، وأضيفت assertions تمنع إعادة إدراج `Escrow` أو `SAIP & WIPO` أو `60M+` في المصدر. يلزم بناء ونشر جديدان قبل اعتماد smoke العام النهائي.

بعد checkpoint `bb462e34` ظل النطاق `https://uplink5.xyz/` يعرض الصفحة الموروثة القديمة في فحصيْن متتالييْن؛ لذلك هذه **مشكلة انتشار/رؤية نشر** وليست دليل قبول. لا يجوز اعتبار smoke الإنتاج PASS حتى يظهر المحتوى الآمن المنشور بالفعل.

بعد إعادة النشر عبر checkpoint `6d137a37` أصبح `/` يعرض الصفحة الآمنة المستقرة: تعريف المحركات الثلاثة، حدود التفويض، تأجيل الذكاء الخارجي، وروابط التسجيل الآمن فقط؛ ولم تظهر الشراكات أو المقاييس أو Escrow أو Blockchain أو تكاملات غير موثقة. لقطة سطح المكتب المستقرة PASS.

فحص `/naqla1` بعد checkpoint حماية المداخل `d0d053a1` أظهر أولاً محتوى legacy ثم شاشة الإقلاع فقط بعد مهلة انتشار ممتدة؛ لا تعدّ أي من الحالتين دليلاً نهائيًا. يلزم تحقق مستقر بعد الإقلاع قبل إغلاق smoke للمسار.

التحقق المستقر اللاحق لـ`/naqla1` PASS: يعرض حاجز تسجيل دخول آمن يصرح بأن السجل والدليل يتطلبان عضوية فعالة وسياقًا نشطًا، ولا يعرض سجلات أو مؤشرات تجريبية. تظل روابط shell عامة ظاهرة، لكنها لا تعرض payload سجلات أو أدلة ضمن هذه الجلسة المجهولة.

التحقق المستقر لـ`/naqla2` PASS: يعرض حاجز تسجيل دخول يبين أن المطابقة والتقديم والتفاعل محكومة بصلاحيات صريحة، ولا يعرض طلبات أو مرشحين أو تقديمات أو مؤشرات تجريبية.

التحقق المستقر لـ`/naqla2/review-assistance` PASS: يعرض رسالة تسجيل الدخول للوصول إلى مساحة طلب أو مراجعة مصرح بها فقط، ولم تظهر تعيينات مراجعة أو مخرجات Copilot أو بيانات مقدمين.

التحقق المستقر لـ`/naqla2/application-assistance` PASS: يعرض الحاجز نفسه في جلسة مجهولة، ولم تظهر طلبات أو توضيحات أو توصيات أو محتوى مراجعة خاص.

التحقق المستقر لـ`/naqla3` PASS: يعرض حاجز تسجيل الدخول للوصول إلى الأصول والمعاملات المصرح بها فقط، ولم يظهر أصل تجاري أو معاملة أو Data Room أو مرحلة تنفيذ.

التحقق المستقر لـ`/naqla3/assets/77` PASS: الرابط العميق لا يكشف metadata أو وثائق أو مفاتيح تخزين أو تفاصيل أصل؛ يعرض حد المصادقة التجاري نفسه فقط.

التحقق المستقر لـ`/naqla3/transactions/77` PASS: الرابط العميق لا يكشف المشاركين أو قرارات العناية أو الشروط أو المعالم أو المخرجات أو مراحل التوسع؛ يعرض حد المصادقة التجاري فقط.

لقطات الجوال المحلية عند `375×812` التقطت الجذر الآمن ورابط الأصل العميق بحالة مستقرة بلا تجاوز أفقي، لكنها التقطت عدة مسارات أخرى في بدايات التحميل أو على لقطة قديمة قبل استقرار التوجيه. لذلك لا تعد هذه اللقطات دليلاً مستقلاً للمسارات؛ دليل الإنتاج المستقر أعلاه هو المرجع لهذه الحواجز، ويلزم عدم الادعاء بتغطية visual mobile للمسارات المؤجلة قبل تحقق مستقر مخصص.

التحقق المستقر لـ`/naqla` PASS: لا ينشئ المدخل العام سياقًا اصطناعيًا أو صلاحية بديلة، ويعرض فقط توجيهًا إلى تسجيل الدخول والسياق النشط.

التحقق المستقر لـ`/naqla2/matching-hub` PASS: لا يعرض طلبات أو نتائج أو عوامل MatchRun في الجلسة المجهولة، ويقصر الوصول على طلبات ونتائج المطابقة المملوكة بعد تسجيل الدخول.
