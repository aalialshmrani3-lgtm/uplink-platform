// @vitest-environment jsdom
import * as React from "react";
import "@testing-library/jest-dom/vitest";
import { afterEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen } from "@testing-library/react";

const authState = { user: null as { id: number } | null, loading: false };
const languageState = { language: "en" as "en" | "ar" };
const workspaceState = { data: null as any };
const assetsState = { data: [] as any[] };
const dueDiligenceRequestsState = { data: [] as any[] };
const managedDocumentsState = { data: [] as any[] };
const sharedDocumentsState = { data: [] as any[] };
const prepareQueryState = { isError: false };
const dueDiligenceQueryState = { isError: false };
const dataRoomQueryState = { isError: false };
const termsState = { data: [{ id: 11, versionNumber: 2, status: "draft", createdBy: 2 }, { id: 12, versionNumber: 1, status: "approved", createdBy: 2 }] as any[], isError: false };
const agreementsState = { data: [{ id: 22, title: "Synthetic agreement", status: "draft", createdBy: 2 }] as any[] };
const executionRecordsState = { data: { plans: [{ id: 1, objectives: "Synthetic execution objectives", status: "active" }], milestones: [{ id: 2, title: "Synthetic milestone", status: "planned" }], deliverables: [{ id: 3, title: "Synthetic submitted deliverable", status: "submitted" }], changeRequests: [{ id: 4, title: "Synthetic open change", details: "Synthetic change details", status: "open" }], risksIssues: [{ id: 5, recordType: "risk", severity: "medium", details: "Synthetic risk details", status: "open" }] } as any, isError: false };
const scaleDecisionsState = { data: [{ id: 41, outcome: "continue_limited", approvedBy: null, createdBy: 2, lessonsLearned: "Synthetic lessons learned" }] as any[], isError: false };
const mutationSpies = { createTermSheet: vi.fn(), approveTermSheet: vi.fn(), setAgreementExecutionStatus: vi.fn(), createExecutionPlan: vi.fn(), createExecutionMilestone: vi.fn(), submitExecutionDeliverable: vi.fn(), acceptExecutionDeliverable: vi.fn(), createExecutionChangeRequest: vi.fn(), resolveExecutionChangeRequest: vi.fn(), recordExecutionRiskIssue: vi.fn(), resolveExecutionRiskIssue: vi.fn(), recordScaleDecision: vi.fn(), approveScaleDecision: vi.fn(), createFollowOnTransaction: vi.fn() };
const downloadFetch = vi.fn();
const query = (data: unknown, isError = false) => ({ data, isLoading: false, isError, refetch: vi.fn() });
vi.mock("@/_core/hooks/useAuth", () => ({ useAuth: () => authState }));
vi.mock("@/contexts/LanguageContext", () => ({ useLanguage: () => languageState }));
vi.mock("@/lib/trpc", () => ({ trpc: { useUtils: () => ({ naqla3: { commercialize: { getDataRoomDocumentDownload: { fetch: downloadFetch } } } }), naqla3: { commercialize: {
  listEligibleInnovationSources: { useQuery: () => query([], prepareQueryState.isError) }, listEligibleEngagements: { useQuery: () => query([], prepareQueryState.isError) }, listAssets: { useQuery: () => query(assetsState.data, prepareQueryState.isError) }, getTransactionWorkspace: { useQuery: () => query(workspaceState.data) }, listTransactionParticipants: { useQuery: () => query([{ userId: 2, organizationId: 3, role: "counterparty" }]) }, listAssetVersions: { useQuery: () => query([]) }, listReadinessVersions: { useQuery: () => query([]) }, listManagedDataRoomDocuments: { useQuery: () => query(managedDocumentsState.data, dataRoomQueryState.isError) }, listDataRoomDocuments: { useQuery: () => query(sharedDocumentsState.data, dataRoomQueryState.isError) }, listDueDiligenceRequests: { useQuery: () => query(dueDiligenceRequestsState.data, dueDiligenceQueryState.isError) }, listTermSheets: { useQuery: () => query(termsState.data, termsState.isError) }, listAgreementRecords: { useQuery: () => query(agreementsState.data, termsState.isError) }, listExecutionRecords: { useQuery: () => query(executionRecordsState.data, executionRecordsState.isError) }, listScaleDecisions: { useQuery: () => query(scaleDecisionsState.data, scaleDecisionsState.isError) },
  createAsset: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, createTransaction: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, createDueDiligenceCase: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, createDueDiligenceRequest: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, respondDueDiligenceRequest: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, reviewDueDiligenceResponse: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, completeDueDiligence: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, transitionStage: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, createAssetVersion: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, createReadinessVersion: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, registerDataRoomDocument: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, shareDataRoomDocument: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, revokeDataRoomDocumentShare: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, createTermSheet: { useMutation: () => ({ isPending: false, mutate: mutationSpies.createTermSheet }) }, approveTermSheet: { useMutation: () => ({ isPending: false, mutate: mutationSpies.approveTermSheet }) }, createAgreementRecord: { useMutation: () => ({ isPending: false, mutate: vi.fn() }) }, setAgreementExecutionStatus: { useMutation: () => ({ isPending: false, mutate: mutationSpies.setAgreementExecutionStatus }) }, createExecutionPlan: { useMutation: () => ({ isPending: false, mutate: mutationSpies.createExecutionPlan }) }, createExecutionMilestone: { useMutation: () => ({ isPending: false, mutate: mutationSpies.createExecutionMilestone }) }, submitExecutionDeliverable: { useMutation: () => ({ isPending: false, mutate: mutationSpies.submitExecutionDeliverable }) }, acceptExecutionDeliverable: { useMutation: () => ({ isPending: false, mutate: mutationSpies.acceptExecutionDeliverable }) }, createExecutionChangeRequest: { useMutation: () => ({ isPending: false, mutate: mutationSpies.createExecutionChangeRequest }) }, resolveExecutionChangeRequest: { useMutation: () => ({ isPending: false, mutate: mutationSpies.resolveExecutionChangeRequest }) }, recordExecutionRiskIssue: { useMutation: () => ({ isPending: false, mutate: mutationSpies.recordExecutionRiskIssue }) }, resolveExecutionRiskIssue: { useMutation: () => ({ isPending: false, mutate: mutationSpies.resolveExecutionRiskIssue }) }, recordScaleDecision: { useMutation: () => ({ isPending: false, mutate: mutationSpies.recordScaleDecision }) }, approveScaleDecision: { useMutation: () => ({ isPending: false, mutate: mutationSpies.approveScaleDecision }) }, createFollowOnTransaction: { useMutation: () => ({ isPending: false, mutate: mutationSpies.createFollowOnTransaction }) },
} } } }));
import CommercializeWorkspace from "./CommercializeWorkspace";

afterEach(() => { cleanup(); vi.clearAllMocks(); });
describe("CommercializeWorkspace", () => {
  it("keeps commercial data absent behind a safe sign-in boundary", () => { authState.user = null; languageState.language = "en"; render(<CommercializeWorkspace />); expect(screen.getByText("Sign in to access only authorized commercial assets and transactions.")).toBeInTheDocument(); expect(screen.queryByText("Commercialize Workspace")).not.toBeInTheDocument(); });
  it("renders human-governed workspace controls without a legal or automatic claim", () => { authState.user = { id: 1 }; languageState.language = "en"; render(<CommercializeWorkspace />); const main = screen.getByRole("main"); expect(main).toHaveAttribute("dir", "ltr"); expect(screen.getByRole("heading", { name: "Commercialize Workspace" })).toBeInTheDocument(); expect(screen.getByText(/without replacing human judgment/i)).toBeInTheDocument(); expect(screen.getByLabelText("Asset title")).toBeInTheDocument(); });
  it("preserves Arabic RTL and mobile overflow contract", () => { authState.user = { id: 1 }; languageState.language = "ar"; render(<CommercializeWorkspace />); const main = screen.getByRole("main"); expect(main).toHaveAttribute("dir", "rtl"); expect(main).toHaveClass("max-w-7xl", "overflow-x-hidden", "px-4"); expect(screen.getByRole("heading", { name: "مساحة التسويق والتنفيذ التجاري" })).toBeInTheDocument(); });
  it("renders an authorized commercial asset deep link with immutable history controls", () => { authState.user = { id: 1 }; languageState.language = "en"; assetsState.data = [{ id: 77, title: "Pinned synthetic asset", summary: "Synthetic pinned commercial summary", status: "prepared", classification: "restricted" }]; window.history.pushState({}, "", "/naqla3/assets/77"); render(<CommercializeWorkspace />); expect(screen.getByRole("heading", { name: "Commercial asset record" })).toBeInTheDocument(); expect(screen.getByText("Pinned synthetic asset")).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Save new asset version" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Add readiness review record" })).toBeInTheDocument(); assetsState.data = []; });
  it("mounts human contract actions and Data Room management only inside an authorized transaction workspace", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["manage_terms", "manage_data_room"] }, dueDiligence: { id: 1 }, termSheet: { id: 2 } }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.getByRole("heading", { name: "Contract" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Save term draft" })).toBeInTheDocument(); expect(screen.getByLabelText("Document title")).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Register document metadata" })).toBeInTheDocument(); expect(screen.getByText(/creates no automatic contract/i)).toBeInTheDocument(); workspaceState.data = null; });
  it("renders separate term approval and external agreement execution controls only for term managers", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["manage_terms"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.getByRole("button", { name: "Approve separately" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Create agreement record" })).toBeInTheDocument(); expect(screen.getByLabelText("External execution reference")).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Record human execution" })).toBeInTheDocument(); workspaceState.data = null; });
  it("submits a governed term draft through the authorized tRPC mutation", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["manage_terms"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); fireEvent.change(screen.getByLabelText("Proposed commercial scope"), { target: { value: "Synthetic commercial scope for human review." } }); fireEvent.change(screen.getByLabelText("Review structure"), { target: { value: "Synthetic review structure for human review." } }); fireEvent.click(screen.getByRole("button", { name: "Save term draft" })); expect(mutationSpies.createTermSheet).toHaveBeenCalledWith({ transactionId: 77, commercialScope: "Synthetic commercial scope for human review.", structure: "Synthetic review structure for human review." }); workspaceState.data = null; });
  it("submits separate term approval and external agreement execution mutations", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["manage_terms"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); fireEvent.change(screen.getByLabelText("Term approval note"), { target: { value: "Synthetic separate approval note." } }); fireEvent.click(screen.getByRole("button", { name: "Approve separately" })); expect(mutationSpies.approveTermSheet).toHaveBeenCalledWith({ termSheetId: 11, approvalNote: "Synthetic separate approval note." }); fireEvent.change(screen.getByLabelText("External execution reference"), { target: { value: "SYNTHETIC-EXTERNAL-REF" } }); fireEvent.click(screen.getByRole("button", { name: "Record human execution" })); expect(mutationSpies.setAgreementExecutionStatus).toHaveBeenCalledWith({ agreementId: 22, status: "executed", externalReference: "SYNTHETIC-EXTERNAL-REF" }); workspaceState.data = null; });
  it("submits a human execution plan and scale decision through their authorized mutations", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "execute", transitionVersion: 4 }, participant: { capabilities: ["manage_execution"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); const { unmount } = render(<CommercializeWorkspace />); fireEvent.change(screen.getByLabelText("Execution objectives"), { target: { value: "Synthetic governed execution objective." } }); fireEvent.click(screen.getByRole("button", { name: "Save execution plan" })); expect(mutationSpies.createExecutionPlan).toHaveBeenCalledWith({ transactionId: 77, objectives: "Synthetic governed execution objective." }); unmount(); workspaceState.data = { transaction: { stage: "scale", transitionVersion: 5 }, participant: { capabilities: ["close_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; render(<CommercializeWorkspace />); fireEvent.change(screen.getByLabelText("Lessons learned"), { target: { value: "Synthetic lessons recorded by a human." } }); fireEvent.click(screen.getByRole("button", { name: "Record human decision" })); expect(mutationSpies.recordScaleDecision).toHaveBeenCalledWith({ transactionId: 77, outcome: "continue_limited", lessonsLearned: "Synthetic lessons recorded by a human." }); workspaceState.data = null; });
  it("hides term creation and Data Room management controls when the participant lacks those capabilities", () => { authState.user = { id: 1 }; languageState.language = "en"; sharedDocumentsState.data = [{ id: 61, title: "Shared synthetic document", documentType: "evidence", classification: "confidential" }]; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["view_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.queryByRole("button", { name: "Save term draft" })).not.toBeInTheDocument(); expect(screen.queryByRole("button", { name: "Register document metadata" })).not.toBeInTheDocument(); expect(screen.getByRole("button", { name: "Authorize download" })).toBeInTheDocument(); sharedDocumentsState.data = []; workspaceState.data = null; });
  it("mounts governed execution controls for a participant with manage_execution", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "execute", transitionVersion: 4 }, participant: { capabilities: ["manage_execution"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.getByRole("button", { name: "Save execution plan" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Add milestone" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Submit deliverable" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Record change request" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Record risk or issue" })).toBeInTheDocument(); workspaceState.data = null; });
  it("submits execution review and record mutations from the visible governed controls", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "execute", transitionVersion: 4 }, participant: { capabilities: ["manage_execution"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); fireEvent.click(screen.getByLabelText("Active plan")); fireEvent.click(screen.getByRole("option", { name: /Synthetic execution objectives/ })); fireEvent.change(screen.getByLabelText("Milestone title"), { target: { value: "Synthetic added milestone" } }); fireEvent.change(screen.getByLabelText("Milestone description"), { target: { value: "Synthetic added milestone description." } }); fireEvent.click(screen.getByRole("button", { name: "Add milestone" })); expect(mutationSpies.createExecutionMilestone).toHaveBeenCalledWith({ transactionId: 77, executionPlanId: 1, title: "Synthetic added milestone", description: "Synthetic added milestone description." }); fireEvent.click(screen.getByLabelText("Milestone")); fireEvent.click(screen.getByRole("option", { name: /Synthetic milestone/ })); fireEvent.change(screen.getByLabelText("Deliverable title"), { target: { value: "Synthetic added deliverable" } }); fireEvent.change(screen.getByLabelText("Deliverable evidence reference"), { target: { value: "synthetic/evidence" } }); fireEvent.click(screen.getByRole("button", { name: "Submit deliverable" })); expect(mutationSpies.submitExecutionDeliverable).toHaveBeenCalledWith({ transactionId: 77, milestoneId: 2, title: "Synthetic added deliverable", evidenceReference: "synthetic/evidence" }); fireEvent.change(screen.getByLabelText("Human acceptance or resolution note"), { target: { value: "Synthetic human review note." } }); fireEvent.click(screen.getByRole("button", { name: "Accept deliverable" })); expect(mutationSpies.acceptExecutionDeliverable).toHaveBeenCalledWith({ deliverableId: 3, acceptanceNote: "Synthetic human review note." }); fireEvent.click(screen.getByRole("button", { name: "Approve change" })); expect(mutationSpies.resolveExecutionChangeRequest).toHaveBeenCalledWith({ changeRequestId: 4, outcome: "approved", resolutionNote: "Synthetic human review note." }); fireEvent.click(screen.getByRole("button", { name: "Resolve record" })); expect(mutationSpies.resolveExecutionRiskIssue).toHaveBeenCalledWith({ recordId: 5, resolutionNote: "Synthetic human review note." }); fireEvent.change(screen.getByLabelText("Change request title"), { target: { value: "Synthetic new change" } }); fireEvent.change(screen.getByLabelText("Change request details"), { target: { value: "Synthetic new change details." } }); fireEvent.click(screen.getByRole("button", { name: "Record change request" })); expect(mutationSpies.createExecutionChangeRequest).toHaveBeenCalledWith({ transactionId: 77, title: "Synthetic new change", details: "Synthetic new change details." }); fireEvent.change(screen.getByLabelText("Risk or issue details"), { target: { value: "Synthetic new risk detail." } }); fireEvent.click(screen.getByRole("button", { name: "Record risk or issue" })); expect(mutationSpies.recordExecutionRiskIssue).toHaveBeenCalledWith({ transactionId: 77, recordType: "risk", severity: "medium", details: "Synthetic new risk detail." }); workspaceState.data = null; });
  it("mounts scale approval and follow-on controls only for the corresponding capabilities", () => { authState.user = { id: 1 }; languageState.language = "en"; scaleDecisionsState.data = [{ id: 41, outcome: "continue_limited", approvedBy: null, createdBy: 2, lessonsLearned: "Synthetic pending decision" }, { id: 42, outcome: "scale", approvedBy: 2, createdBy: 2, lessonsLearned: "Synthetic approved decision" }]; workspaceState.data = { transaction: { stage: "scale", transitionVersion: 5 }, participant: { capabilities: ["close_transaction", "create_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.getByRole("button", { name: "Record human decision" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Approve separately" })).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Open follow-on transaction" })).toBeInTheDocument(); scaleDecisionsState.data = [{ id: 41, outcome: "continue_limited", approvedBy: null, createdBy: 2, lessonsLearned: "Synthetic lessons learned" }]; workspaceState.data = null; });
  it("submits scale approval and follow-on mutations only from authorized controls", () => { authState.user = { id: 1 }; languageState.language = "en"; scaleDecisionsState.data = [{ id: 41, outcome: "continue_limited", approvedBy: null, createdBy: 2, lessonsLearned: "Synthetic pending decision" }, { id: 42, outcome: "scale", approvedBy: 2, createdBy: 2, lessonsLearned: "Synthetic approved decision" }]; workspaceState.data = { transaction: { stage: "scale", transitionVersion: 5 }, participant: { capabilities: ["close_transaction", "create_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); fireEvent.change(screen.getByLabelText("Approval or closure note"), { target: { value: "Synthetic separate scale approval." } }); fireEvent.click(screen.getByRole("button", { name: "Approve separately" })); expect(mutationSpies.approveScaleDecision).toHaveBeenCalledWith({ decisionId: 41, closureNote: "Synthetic separate scale approval." }); fireEvent.click(screen.getByRole("button", { name: "Open follow-on transaction" })); expect(mutationSpies.createFollowOnTransaction).toHaveBeenCalledWith(expect.objectContaining({ decisionId: 42 })); scaleDecisionsState.data = [{ id: 41, outcome: "continue_limited", approvedBy: null, createdBy: 2, lessonsLearned: "Synthetic lessons learned" }]; workspaceState.data = null; });
  it("reauthorizes a shared Data Room download through the tRPC fetch path without storage-key rendering", async () => { authState.user = { id: 1 }; languageState.language = "en"; sharedDocumentsState.data = [{ id: 61, title: "Shared synthetic document", documentType: "evidence", classification: "confidential" }]; downloadFetch.mockResolvedValueOnce({ title: "Shared synthetic document", url: "/authorized-synthetic-download" }); workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["view_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); fireEvent.click(screen.getByRole("button", { name: "Authorize download" })); expect(downloadFetch).toHaveBeenCalledWith({ documentId: 61 }); expect(await screen.findByRole("link", { name: "Shared synthetic document" })).toHaveAttribute("href", "/authorized-synthetic-download"); expect(screen.queryByText(/storageKey/i)).not.toBeInTheDocument(); sharedDocumentsState.data = []; workspaceState.data = null; });
  it("mounts participant-bound Due Diligence actions inside an authorized transaction workspace", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "due_diligence", transitionVersion: 2 }, participant: { capabilities: ["request_information", "manage_due_diligence"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.getByRole("heading", { name: "Due Diligence" })).toBeInTheDocument(); expect(screen.getByLabelText("Authorized recipient")).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Send information request" })).toBeInTheDocument(); workspaceState.data = null; });
  it("renders explicit retry states when Due Diligence or Data Room metadata fails", () => { authState.user = { id: 1 }; languageState.language = "en"; dueDiligenceQueryState.isError = true; workspaceState.data = { transaction: { stage: "due_diligence", transitionVersion: 2 }, participant: { capabilities: ["view_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); const { unmount } = render(<CommercializeWorkspace />); expect(screen.getByRole("alert")).toHaveTextContent("Authorized Due Diligence requests could not be loaded."); expect(screen.getByRole("button", { name: "Retry" })).toBeInTheDocument(); unmount(); dueDiligenceQueryState.isError = false; dataRoomQueryState.isError = true; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["view_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; render(<CommercializeWorkspace />); expect(screen.getByRole("alert")).toHaveTextContent("Authorized Data Room metadata could not be loaded."); dataRoomQueryState.isError = false; workspaceState.data = null; });
  it("renders explicit retry states when Prepare, Contract, Execution, or Scale reads fail", () => { authState.user = { id: 1 }; languageState.language = "en"; prepareQueryState.isError = true; workspaceState.data = null; window.history.pushState({}, "", "/naqla3"); const { unmount } = render(<CommercializeWorkspace />); expect(screen.getByRole("alert")).toHaveTextContent("Authorized Prepare sources, assets, or engagements could not be loaded."); unmount(); prepareQueryState.isError = false; termsState.isError = true; workspaceState.data = { transaction: { stage: "contract", transitionVersion: 3 }, participant: { capabilities: ["manage_terms"] }, dueDiligence: { id: 1 }, termSheet: null }; window.history.pushState({}, "", "/naqla3/transactions/77"); const contract = render(<CommercializeWorkspace />); expect(screen.getByRole("alert")).toHaveTextContent("Authorized term drafts or agreement records could not be loaded."); contract.unmount(); termsState.isError = false; executionRecordsState.isError = true; workspaceState.data = { transaction: { stage: "execute", transitionVersion: 4 }, participant: { capabilities: ["manage_execution"] }, dueDiligence: { id: 1 }, termSheet: null }; const execution = render(<CommercializeWorkspace />); expect(screen.getByRole("alert")).toHaveTextContent("Authorized execution records could not be loaded."); execution.unmount(); executionRecordsState.isError = false; scaleDecisionsState.isError = true; workspaceState.data = { transaction: { stage: "scale", transitionVersion: 5 }, participant: { capabilities: ["close_transaction"] }, dueDiligence: { id: 1 }, termSheet: null }; render(<CommercializeWorkspace />); expect(screen.getByRole("alert")).toHaveTextContent("Authorized scale decisions could not be loaded."); scaleDecisionsState.isError = false; workspaceState.data = null; });
  it("renders the recipient response control without exposing another participant's requests", () => { authState.user = { id: 1 }; languageState.language = "en"; workspaceState.data = { transaction: { stage: "due_diligence", transitionVersion: 2 }, participant: { capabilities: ["respond_due_diligence"] }, dueDiligence: { id: 1 }, termSheet: null }; dueDiligenceRequestsState.data = [{ id: 31, recipientUserId: 1, subject: "Synthetic request", body: "Synthetic authorized request body", status: "sent" }]; window.history.pushState({}, "", "/naqla3/transactions/77"); render(<CommercializeWorkspace />); expect(screen.getByLabelText("Due Diligence response")).toBeInTheDocument(); expect(screen.getByRole("button", { name: "Submit response" })).toBeInTheDocument(); workspaceState.data = null; dueDiligenceRequestsState.data = []; });
});
