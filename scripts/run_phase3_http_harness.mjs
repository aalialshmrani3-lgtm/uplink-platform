import { createServer } from "node:net"; import { mkdir,mkdtemp,readFile,readdir,rm } from "node:fs/promises"; import { tmpdir } from "node:os"; import path from "node:path"; import { randomBytes } from "node:crypto"; import { spawn,spawnSync } from "node:child_process"; import mysql from "mysql2/promise";
const root=path.resolve(import.meta.dirname,".."), base=await mkdtemp(path.join(tmpdir(),"naqla3-mariadb-")), data=path.join(base,"data"), socket=path.join(base,"db.sock"), pid=path.join(base,"db.pid"), log=path.join(base,"db.log"), name=`naqla_phase3_test_${randomBytes(5).toString("hex")}`, user="naqla3", password=randomBytes(18).toString("hex"); let daemon; const wait=n=>new Promise(r=>setTimeout(r,n)); const port=await new Promise((resolve,reject)=>{const s=createServer();s.once("error",reject);s.listen(0,"127.0.0.1",()=>{const a=s.address();s.close(e=>e?reject(e):resolve(a.port));});});
async function ready(){for(let i=0;i<80;i++){try{const c=await mysql.createConnection({host:"127.0.0.1",port,user:"root"});await c.end();return;}catch{await wait(125);}}throw new Error(await readFile(log,"utf8").catch(()=>"TEMP_MARIADB_NOT_READY"));} async function migrate(){const c=await mysql.createConnection({host:"127.0.0.1",port,user:"root",multipleStatements:true});try{await c.query(`CREATE DATABASE \`${name}\``);await c.query(`CREATE USER '${user}'@'127.0.0.1' IDENTIFIED BY '${password}'`);await c.query(`GRANT ALL PRIVILEGES ON \`${name}\`.* TO '${user}'@'127.0.0.1'`);await c.changeUser({database:name});for(const file of (await readdir(path.join(root,"drizzle"))).filter(f=>/^\d{4}_.+\.sql$/.test(f)).sort()){const text=(await readFile(path.join(root,"drizzle",file),"utf8")).replaceAll("--> statement-breakpoint","");if(text.trim())await c.query(text);}}finally{await c.end();}}
try{await mkdir(data,{recursive:true});if(spawnSync("/usr/bin/mariadb-install-db",[`--datadir=${data}`,"--auth-root-authentication-method=normal","--skip-test-db","--user=ubuntu"],{stdio:"ignore"}).status!==0)throw new Error("TEMP_MARIADB_INITIALIZATION_FAILED");daemon=spawn("/usr/sbin/mariadbd",[`--datadir=${data}`,`--socket=${socket}`,`--pid-file=${pid}`,`--log-error=${log}`,`--port=${port}`,"--bind-address=127.0.0.1","--skip-name-resolve","--user=ubuntu"],{stdio:"ignore"});await ready();await migrate();const databaseUrl=`mysql://${user}:${password}@127.0.0.1:${port}/${name}`;const run=spawn("pnpm",["tsx","scripts/phase3_http_harness.ts"],{cwd:root,env:{...process.env,NODE_ENV:"test",DATABASE_URL:databaseUrl,NAQLA_TEST_DATABASE_URL:databaseUrl,AI_EXTERNAL_PROVIDER_ENABLED:"false"},stdio:"inherit"});const code=await new Promise(r=>run.once("exit",c=>r(c??1)));if(code!==0)throw new Error("NAQLA3_HTTP_HARNESS_FAILED");}finally{if(daemon&&!daemon.killed){daemon.kill("SIGTERM");await Promise.race([new Promise(r=>daemon.once("exit",r)),wait(5000)]);if(daemon.exitCode===null)daemon.kill("SIGKILL");}await rm(base,{recursive:true,force:true});}console.log(JSON.stringify({cleanup:"PASS",synthetic:true,externalProviderCalls:0}));
