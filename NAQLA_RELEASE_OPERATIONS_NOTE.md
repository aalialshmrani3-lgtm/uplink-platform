# ملاحظة تشغيلية — NAQLA Platform RC1

| المجال | الإجراء التشغيلي |
|---|---|
| النشر | احفظ checkpoint بعد `pnpm build` الناجح؛ النشر التلقائي هو مسار الإصدار لهذا المشروع |
| الهجرات | راجع SQL يدويًا ولا تشغّل generate واسعًا عند ظهور rename تاريخي غير متعلق؛ اختبر سلسلة migrations في MariaDB محلية أولاً |
| الاستعادة | استخدم checkpoint منشورًا عبر واجهة الإدارة عند فشل تغيير؛ لا تستخدم reset شاملًا |
| البيانات | لا تستخدم قاعدة الإنتاج في Harness؛ يستلزم الاختبار `NAQLA_TEST_DATABASE_URL` محليًا عشوائيًا فقط |
| الأسرار | تبقى خارج المستودع وملفات الحزم؛ لا تسجل values في تقارير القبول |
| الذكاء الخارجي | الحالة `DEFERRED`، والبوابة تبقى `AI_EXTERNAL_PROVIDER_ENABLED=false` |
| الملفات | التخزين عبر المرجع الخادمي؛ بيانات Data Room تحتاج تفويضًا عند العرض والتنزيل |
